---
title: API Arch
---
:::caution
(Novembre 2018) Le API di Arch sono elencate inautogenerated API documentation.
:::

Le funzioni API fanno parte dell'ambiente [Arch](/Arch_Workbench/it "Arch Workbench/it") e possono essere usate nelle [macro](/Macros/it "Macros/it") e dalla console [Python](/Python/it "Python/it") dopo che il modulo `Arch/it` è stato importato. L'Ambiente [Reinforcement](/Reinforcement_Workbench/it "Reinforcement Workbench/it") ha le sue [Reinforcement API](/Reinforcement_API/it "Reinforcement API/it").

Esempio:

```
import FreeCAD, Draft, Arch

p1 = FreeCAD.Vector(0, 0, 0)
p2 = FreeCAD.Vector(2000, 0, 0)
baseline = Draft.makeLine(p1, p2)

Arch.makeWall(baseline, length=None, width=200, height=2000)

```

Retrieved from "<http://wiki.freecad.org/index.php?title=Arch_API/it&oldid=1432708>"